# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET     /                           controllers.HomeController.index(request: Request)
POST    /                           controllers.HomeController.sendMail(request: Request)
